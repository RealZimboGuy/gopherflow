// Package workflow_helpers provides helpers for carrying structured data in a
// workflow's state variables, which are stored as a map of strings. Use
// [SaveStructToStateVars] and [LoadStructFromStateVars] to round-trip a struct
// through that map as JSON.
package workflow_helpers
