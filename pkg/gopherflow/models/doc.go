// Package models holds the types exchanged with workflow code and the REST API:
// the state a workflow returns, its declared states and retry policy, and the
// request and response bodies for the API.
package models
