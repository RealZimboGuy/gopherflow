// Package domain holds the persisted types the engine reads and writes:
// workflows and their action history, workflow definitions, executors and
// users. These map directly onto the database schema.
package domain
